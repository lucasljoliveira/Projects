<p id="notice"><%= notice %></p>

<h1>Enderecos</h1>

<table>
  <thead>
    <tr>
      <th>Pessoa</th>
      <th>Cep</th>
      <th>Pais</th>
      <th>Uf</th>
      <th>Cidade</th>
      <th>Bairro</th>
      <th>Rua</th>
      <th>Numero</th>
      <th>Complemento</th>
      <th>Tipo moradia</th>
      <th>Num comodos</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @enderecos.each do |endereco| %>
      <tr>
        <td><%= endereco.Pessoa %></td>
        <td><%= endereco.cep %></td>
        <td><%= endereco.pais %></td>
        <td><%= endereco.uf %></td>
        <td><%= endereco.cidade %></td>
        <td><%= endereco.bairro %></td>
        <td><%= endereco.rua %></td>
        <td><%= endereco.numero %></td>
        <td><%= endereco.complemento %></td>
        <td><%= endereco.tipo_moradia %></td>
        <td><%= endereco.num_comodos %></td>
        <td><%= link_to 'Show', endereco %></td>
        <td><%= link_to 'Edit', edit_endereco_path(endereco) %></td>
        <td><%= link_to 'Destroy', endereco, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Endereco', new_endereco_path %>
